version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/source/conf.py

python:
  install:
    # 安装 Sphinx 及文档依赖
    - requirements: docs/requirements.txt
    # 安装 SDK 本身（使 autodoc 能 import 源码及其依赖）
    - method: pip
      path: .
